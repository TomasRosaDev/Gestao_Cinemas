package Inferfaces;

import javax.swing.*;
import java.awt.*;

public abstract class BotLabel extends JPanel {

    public BotLabel(){
        this.setPreferredSize(new Dimension(600,50));
        this.setBackground(Color.black);
    }
}
